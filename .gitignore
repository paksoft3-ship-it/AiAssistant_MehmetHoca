node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Delivery archives (kept locally, not tracked)
_zip_archive/
*.zip

# Waitlist signups (server-side, not tracked)
data/

# Raw Stitch design export (reference only; distilled into docs/DESIGN.md)
stitch_eidosus_ui_design_system/
